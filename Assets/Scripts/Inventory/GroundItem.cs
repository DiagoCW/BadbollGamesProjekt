using UnityEngine;

public class GroundItem : MonoBehaviour
{
    // this script is used for items that are on the ground, it allows the player
    // to pick them up by walking over them. It is currently not used, but it is still
    // in the project in case we want to use it later. -Hugo

    public ItemObject item;
}
